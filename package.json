{
  "name": "fastcampus-front-projects",
  "version": "1.0.0",
  "workspaces": [
    "packages/**/*",
    "services/**/*"
  ],
  "scripts": {
    "build": "nx build",
    "graph": "nx run-many --target=build --graph"
  },
  "packageManager": "yarn@4.6.0",
  "devDependencies": {
    "nx": "^16.8.1"
  }
}
